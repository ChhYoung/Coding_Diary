clear all;close all;
im = imread('./pics/Fig10.25.jpg');
im = rgb2gray(im);
subplot(1,3,1),imshow(im);
im = medfilt2(im,[3,3]);
subplot(1,3,2),imhist(im);
T = graythresh(im);
im = imbinarize(im,T);
subplot(1,3,3),imshow(im);
A = 