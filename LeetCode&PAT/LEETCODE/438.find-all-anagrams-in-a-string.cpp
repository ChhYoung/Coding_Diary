/*
 * @lc app=leetcode id=438 lang=cpp
 *
 * [438] Find All Anagrams in a String
 */
#include<string>
#include<vector>
using namespace std;

class Solution {

public:
    vector<int> findAnagrams(string s, string p) {
        vector<int> res;
        bool same_elem = true;
        int len_s = s.length(),len_p = p.length();
        for(int i=0;i<=s.length()-len_p;){
            
        }
    }
};

